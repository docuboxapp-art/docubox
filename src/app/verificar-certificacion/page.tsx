'use client';
import { useState } from 'react';
import { Search, ShieldCheck } from 'lucide-react';
import { useRouter } from 'next/navigation';
export default function PublicVerifierPage() { const router = useRouter(); const [value, setValue] = useState(''); return <main className="min-h-screen bg-[#f6f8fb] px-4 py-12"><div className="mx-auto max-w-2xl"><div className="text-center"><span className="mx-auto flex h-12 w-12 items-center justify-center rounded-lg bg-blue-50 text-primary"><ShieldCheck size={24} /></span><h1 className="mt-4 text-2xl font-semibold">Verificar certificacion</h1><p className="mt-2 text-sm leading-6 text-slate-500">Ingresa la referencia segura incluida en la constancia o abre directamente su codigo QR.</p></div><form onSubmit={(e) => { e.preventDefault(); const token = value.trim().split('/').pop(); if (token) router.push(`/verificar-certificacion/c/${token}`); }} className="mt-8 rounded-lg border border-slate-200 bg-white p-5"><label className="text-sm font-semibold">Referencia de verificacion</label><div className="mt-2 flex gap-2"><input value={value} onChange={(e) => setValue(e.target.value)} placeholder="Pega la URL o el identificador" className="h-10 flex-1 rounded-md border border-slate-200 px-3 text-sm outline-none focus:border-primary" /><button className="inline-flex h-10 items-center gap-2 rounded-md bg-primary px-4 text-sm font-semibold text-white"><Search size={15} />Verificar</button></div></form><p className="mt-6 text-center text-xs text-slate-400">La consulta no expone el documento original ni datos personales.</p></div></main>; }

