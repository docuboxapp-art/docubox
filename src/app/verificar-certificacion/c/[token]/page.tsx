'use client';
import { useEffect, useState } from 'react';
import { useParams } from 'next/navigation';
import { AlertTriangle, CheckCircle2, Fingerprint, ShieldCheck } from 'lucide-react';
export default function PublicCertificationPage() { const { token } = useParams<{ token: string }>(); const [data, setData] = useState<any>(null); const [error, setError] = useState(''); useEffect(() => { fetch(`/api/public/certifica/${token}`, { cache: 'no-store' }).then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error); return body; }).then(setData).catch((cause) => setError(cause.message)); }, [token]); if (error) return <State valid={false} title="No fue posible verificar" message={error} />; if (!data) return <main className="flex min-h-screen items-center justify-center bg-[#f6f8fb]"><span className="h-8 w-8 animate-spin rounded-full border-2 border-primary/20 border-t-primary" /></main>; const item = data.certification; const sandbox = item.provider_mode === 'sandbox' && item.service_key !== 'integrity'; return <main className="min-h-screen bg-[#f6f8fb] px-4 py-10"><div className="mx-auto max-w-4xl"><header className="rounded-lg border border-slate-200 bg-white p-6 text-center"><span className={`mx-auto flex h-14 w-14 items-center justify-center rounded-full ${sandbox ? 'bg-amber-50 text-amber-700' : 'bg-emerald-50 text-emerald-700'}`}>{sandbox ? <AlertTriangle size={26} /> : <CheckCircle2 size={26} />}</span><h1 className="mt-4 text-2xl font-semibold">{sandbox ? 'Evidencia de demostracion' : 'Certificacion verificada'}</h1><p className="mt-1 text-sm text-slate-500">{item.human_folio} · {item.title}</p>{sandbox && <p className="mx-auto mt-4 max-w-xl rounded-md border border-amber-200 bg-amber-50 p-3 text-sm font-semibold text-amber-900">NO VALIDO / DEMOSTRACION · No es una constancia emitida por un PSC acreditado.</p>}</header><div className="mt-4 grid gap-4 md:grid-cols-2"><Panel title="Integridad"><Row label="Huella original" value={item.original_sha256} mono /><Row label="Huella final" value={item.final_sha256} mono /><Row label="Coincidencia" value={data.verification.original_hash_matches_final ? 'Coincide' : 'No coincide'} /></Panel><Panel title="Emision"><Row label="Servicio" value={item.service_key} /><Row label="Entorno" value={item.provider_mode} /><Row label="Fecha" value={item.issued_at ? new Date(item.issued_at).toLocaleString('es-MX') : 'No disponible'} /></Panel><Panel title="Manifiesto"><Row label="Version" value={data.manifest?.schema_version || 'No disponible'} /><Row label="SHA-256" value={data.manifest?.canonical_sha256 || 'No disponible'} mono /></Panel><Panel title="Evidencias"><Row label="Registros" value={String(data.evidences.length)} /><Row label="Resultado" value={data.verification.overall_status.replaceAll('_', ' ')} /></Panel></div><p className="mt-5 text-center text-xs text-slate-400">Verificado por Docubox Certifica · {new Date(data.verification.checked_at).toLocaleString('es-MX')}</p></div></main>; }
function State({ valid, title, message }: { valid: boolean; title: string; message: string }) { return <main className="flex min-h-screen items-center justify-center bg-[#f6f8fb] p-4"><div className="max-w-md rounded-lg border border-slate-200 bg-white p-8 text-center"><ShieldCheck className="mx-auto text-slate-400" /><h1 className="mt-4 text-xl font-semibold">{title}</h1><p className="mt-2 text-sm text-slate-500">{message}</p></div></main>; }
function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-lg border border-slate-200 bg-white"><header className="flex items-center gap-2 border-b border-slate-200 px-4 py-3"><Fingerprint size={15} className="text-primary" /><h2 className="text-sm font-semibold">{title}</h2></header><dl className="space-y-3 p-4">{children}</dl></section>; }
function Row({ label, value, mono }: { label: string; value: string; mono?: boolean }) { return <div><dt className="text-xs font-semibold text-slate-400">{label}</dt><dd className={`mt-1 break-all text-sm ${mono ? 'font-mono text-xs' : 'font-medium'}`}>{value || 'No disponible'}</dd></div>; }
